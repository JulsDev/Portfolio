.hero{
  height: 100vh;
}

.hero__container{
  display: flex;
  flex-direction: column;
  height: 100%;
  padding: 65px 20px;
}

.hero__content{
  flex: 1;
  display: flex;
  justify-content: center;
  align-items: center;
}

.user-info{
  display: flex;

  @include tablets(){
    flex-direction: column;
    justify-content: center;
  }
}

.user-info__lastName {
  margin-top: 10px;
  margin-left: 14px;
}

.user-info__descr{
  text-align: right;
  margin-right: 60px;
  font-size: 16px;
  font-weight: 700;
  line-height: 30px;
  opacity: .4;

  @include tablets(){
    text-align: center;
    margin-right: 0;
    margin-bottom: 30px;
  }
}

.user-info__name{
  font-size: 69px;
  font-weight: 800;
  line-height: 60px;

  @include tablets(){
    text-align: center;
    font-size: 56px;
    line-height: 48px;
  }
  @include phones(){
    font-size: 48px;
  }
}

.hero__scroll-button{
  display: inline-flex;
  vertical-align: middle;
  justify-content: center;

  &:hover{
    color: $hover-color;
    .scroll__icon{
      fill: $hover-color;
    }
  }

  @include phones(){
    display: none;
  }
}

.scroll-down-button{
  display: inline-flex;
  vertical-align: middle;
  align-items: center;
  margin-right: 15px;
  font-family: "Open Sans";
  font-size: 16px;
  font-weight: 600;
}

.scroll__icon{
  width: 1.55rem;
  height: 30px;
  fill: $text-color;
}